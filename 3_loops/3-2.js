// Выведете на экран 10 раз слово "Привет". Каждое слово "Привет" должно быть на новой строке, вот так:

let str = "";

for (let i = 0; i < 10; i++) {
    str = str + "Привет\n";
}

console.log(str);
